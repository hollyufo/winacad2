import java.io.BufferedReader;
import java.io.InputStreamReader;
import java.util.LinkedList;

public class college {
    LinkedList collages = new LinkedList();
    public void addCollage() {
        System.out.println("Enter the name of the collage: ");
        BufferedReader Name = new BufferedReader(new InputStreamReader(System.in));
        System.out.println("Enter the Web site of the collage: ");
        BufferedReader Website = new BufferedReader(new InputStreamReader(System.in));
        // creating an array

    }
}
